import 'package:flutter/material.dart';

class AddonsItems extends StatelessWidget {
  const AddonsItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
